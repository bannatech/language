func test (int x, int y, int z) -> int:
{
	int c = (x * y) + z;
	if c > 3:
	{
		c = c - 3;
	}
	return(c);
}

int x = 3;

string y = "strings";

if x == 3:
{
	print("Potatoes are good for \"the\" soul.");
}

int z = test(3, test(2*3, 2, 6), 1 * 2);

print(y);
print(z);
