include fibb;

func fibbio(int target) -> int:
{
	FullFibb fibb = new FullFibb();

	if target > 1:
	{
		return fibbio(target - 1) + fibbio(target - 2);
	}
	else:
	{
		return fibb.do(2);
	}
}

print fibbio(20);
